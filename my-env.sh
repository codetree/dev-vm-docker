#!/bin/bash

export DEV_PATH=$HOME/repos

alias b='bundle'
alias bi='bundle install'
alias be='bundle exec'
alias bu='bundle update'
alias bs='bundle show'
alias ls='ls --color=auto -G'
alias ll='ls --color=auto -G -al'

